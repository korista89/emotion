# 깃허브 반영 가이드

아래 단계는 현재 로컬 저장소의 변경 사항을 깃허브 원격 저장소에 반영하는 일반적인 절차입니다.

## 1. 원격 저장소 확인 및 추가
1. 현재 원격 목록 확인: `git remote -v`
2. 원격이 없다면 추가: `git remote add origin https://github.com/<사용자>/<저장소>.git`

> 이미 원격이 연결되어 있다면 이 단계는 건너뛰어도 됩니다.

## 2. 변경 사항 확인
1. 작업 현황 확인: `git status`
2. 변경 파일 내용 검토: `git diff`

## 3. 커밋 만들기
1. 커밋할 파일 선택: `git add <파일경로>`
   - 모든 변경을 추가하려면 `git add .`
2. 커밋 작성: `git commit -m "설명 메시지"`

## 4. 브랜치 관리 (선택)
1. 새 브랜치를 만들고 이동: `git checkout -b feature/my-change`
2. 브랜치 목록 확인: `git branch`

> 팀 규칙에 따라 메인 브랜치 대신 기능 브랜치에서 작업한 뒤 PR을 만드는 것이 일반적입니다.

## 5. 깃허브에 푸시
1. 현재 브랜치를 푸시: `git push origin <브랜치명>`
2. 최초 푸시 후 추적 설정 메시지가 나오면 안내에 따라 `git push --set-upstream origin <브랜치명>` 실행

## 6. 깃허브에서 PR 생성
1. 깃허브 웹사이트에서 해당 저장소로 이동
2. 푸시한 브랜치에 대해 "Compare & pull request" 버튼 클릭
3. 변경 사항 설명을 작성하고 PR 생성

## 7. 리뷰 및 병합
1. 팀원 리뷰 후 승인되면 "Merge" 버튼으로 메인 브랜치에 병합
2. 병합 후 로컬 메인 브랜치를 최신 상태로 동기화: `git checkout main` → `git pull`

---

### 추가 팁
- 커밋 전에 `git status`로 불필요한 파일이 포함되지 않았는지 확인하세요.
- 각 커밋 메시지는 변경 내용을 명확하게 설명하면 추후 추적이 쉽습니다.
- PR 생성 시 변경 의도, 테스트 결과, 영향 범위를 명시하면 리뷰 시간이 단축됩니다.

